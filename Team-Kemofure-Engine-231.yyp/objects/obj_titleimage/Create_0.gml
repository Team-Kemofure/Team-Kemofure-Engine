/// @description Initialization

visible = false;			// Set the visibility to false
alarm[0] = room_speed;		// Set the first timer to a second
alarm[1] = room_speed * 4;	// Set the second timer to four seconds
enableInteraction = false;	// Are we able to skip the cutscene?
enableDrawing = false;		// Should we draw the text?